# Homework 2 · Hint 与 Explore Mode

## 一、作业背景

在 Homework 1.1 中，你已经完成了 `Sudoku` / `Game` 的基本对象设计，并实现了 Undo / Redo。

本次作业将在已有设计之上，继续增加两个能力：

1. **提示（Hint）**
2. **探索（Explore）**

本次作业的重点是：  
在已有对象模型上，理解并实践“功能演进”所带来的设计变化，进一步体会：

- 对象之间的协作
- 状态切换
- 分支与回滚
- 设计模式在交互系统中的实际用途

## 二、作业目标

请在 Homework 1 的基础上，完成以下目标：

1. 增加提示功能
2. 增加尝试 / 探索模式
3. 保持对象模型整体一致
4. 通过文档说明本次新增功能如何影响你的设计

---

## 三、作业前提

本次作业在你之前的 Homework 1.1 基线版本之上，做必要的更新。

你需要继续保留并正确使用：

- `Sudoku` / `Game`
- history 机制
- Undo / Redo
- 序列化 / 外表化能力

---

## 四、功能要求

## A. 提示（Hint）

#### 1. 候选提示

提示用户当前棋盘，某个格子的候选数集合。

#### 2. 下一步提示

提示用户当前棋盘，下一步可以填写的候选数（推定数）。

---

### 要求

1. 提示功能必须通过你的领域对象接口提供，而不是仅在 UI 组件中临时拼接。
2. 需要说明：
   - 提示能力属于 `Sudoku`
   - 还是属于 `Game`
   - 或者两者如何协作

---

### 加分项（可选）

你可以进一步支持：

- 提示原因说明
- 多等级提示
- 区分“仅提示位置”和“直接填写答案”

---

## B. 探索（Explore）

当前棋盘所有单元格若都无法推断出推定値（唯一的候选值），则用户可以采取探索模式，在多个候选值中逐一尝试。

----

### 要求

1. 冲突：能够判断探索失败，即棋盘出现了冲突
2. 回溯：用户能够在多次填写后，快速回到探索的起点，选择另外的候选值，进行探索
3. 记忆：用户多路径探索到已经失败的探索路径的某一棋盘时，告知用户探索失败

---

## 五、设计要求

本次作业要求你显式思考以下问题，并在文档中说明：

### 1. 探索模式的本质是什么？

你可以选择以下任一思路：

- `Game` 进入一种新的状态
- `Game` 创建一个临时子会话
- 使用局面快照与回滚机制
- 其他合理方案

### 2. 主局面与探索局面的关系是什么？

你需要说明：

- 是共享对象，还是复制对象？
- 是否会产生深拷贝问题？
- 提交时如何合并？
- 放弃时如何回滚？

### 3. history 如何演进？

你需要说明：

- 探索过程是否拥有独立 history
- 提交后如何进入主 history
- 是否仍然使用线性栈
- 是否引入了树状分支

---

## 六、本次作业的建议设计方向

### 核心对象不建议推倒重来

请尽量在 Homework 1 的对象模型上演进，而不是完全推翻重写。

### 建议保持以下原则

- `Sudoku` 仍然是核心领域对象
- `Game` 仍然负责会话、状态与历史
- 新增功能应尽量体现在已有对象的演进中

### 不建议的方向

- 为了做提示或探索，引入大量与 UI 强耦合的新对象
- 将关键逻辑重新写回组件中
- 过早引入复杂 DAG 合并语义

### 建议利用Coding Agent

- 用CA实现，但必须清楚知晓技术原理
- 用CA支持求解数独

---

## 七、提交物

请提交以下内容：

### 1. 代码

在 GitHub Classroom 分配给你的仓库中完成实现。

### 2. `EVOLUTION.md`

请在仓库根目录提交 `EVOLUTION.md`，至少回答以下问题：

1. 你如何实现提示功能？
2. 你认为提示功能更属于 `Sudoku` 还是 `Game`？为什么？
3. 你如何实现探索模式？
4. 主局面与探索局面的关系是什么？
5. 你的 history 结构在本次作业中是否发生了变化？
6. Homework 1 中的哪些设计，在 Homework 2 中暴露出了局限？
7. 如果重做一次 Homework 1，你会如何修改原设计？

---

## 八、评分标准

总分：100 分

### 1. 提示功能正确性（20 分）

- 提示功能可用
- 行为稳定
- 接口清晰

### 2. 探索模式正确性（25 分）

- 能进入探索
- 能提交探索结果
- 能放弃探索结果
- 主局面行为正确

### 3. 状态与对象协作设计（20 分）

- `Sudoku` / `Game` 关系清晰
- 新增状态或分支设计合理
- 设计未明显退化为“临时拼接”

### 4. history 演进合理性（15 分）

- history 行为自洽
- 回滚 / 提交逻辑合理
- 无明显引用污染问题

### 5. 代码质量（10 分）

- 命名清晰
- 结构清楚
- 可维护性较好

### 6. 文档质量（10 分）

- `EVOLUTION.md` 论述清晰
- 能反思设计演进

---

## 九、限制与说明

### 必须满足

- Homework 1 中已有的 Undo / Redo 不应因本次改动而失效
- 不允许仅通过 UI 层临时变量伪造探索模式
- 提示与探索必须体现在对象设计中

### 不要求

本次作业不要求：

- 多层嵌套探索
- DAG 合并
- 高级求解算法实现
- Svelte 5 升级

---

## 十、加分项（可选）

最多加 10 分：

1. 支持树状探索分支
2. 支持探索过程中的独立 Undo / Redo
3. 提示功能具有解释能力
4. 为探索模式提供更优雅的状态建模
5. 具有较完整测试
6. 利用了AI Agent求解或解释

---

## 十一、建议开发顺序

建议按以下顺序推进：

1. 明确提示接口属于谁
2. 先完成最小提示功能
3. 再设计 Explore Mode 的最小状态流
4. 实现进入探索
5. 实现提交与放弃
6. 校验 Undo / Redo 是否仍然正确
7. 完成 `EVOLUTION.md`

---

## 十二、课堂讨论准备

完成作业后，请准备回答以下问题：

1. 为什么“提示”和“探索”会推动原有对象设计发生变化？
2. 你的探索模式更接近“状态切换”还是“分支会话”？
3. 你的设计最脆弱的地方在哪里？
4. 你的 history 结构是否还能继续扩展？
5. 如果将来迁移到新的响应式机制，你的设计中哪些部分最容易迁移，哪些最难？
